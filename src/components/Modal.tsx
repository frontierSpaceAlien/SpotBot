export function AddTo() {
  const newRow = {
    combo: 'New Combo',
    damage: 50,
    advantage: 10,
    notes: 'Newly added combo',
  }
  return newRow
}

export function Modal() {
  return <div>This is the modal</div>
}
